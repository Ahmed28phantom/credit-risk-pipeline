# Runtime dependencies for the deployed Streamlit app (app/app.py).
# Pinned to the exact versions the model artifact was trained and pickled with
# (see app/artifacts/credit_risk_model.metadata.json) so unpickling stays safe.
streamlit==1.58.0
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.9.0
imbalanced-learn==0.14.2
joblib==1.5.3
